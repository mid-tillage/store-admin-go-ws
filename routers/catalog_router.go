package routers

import (
	"github.com/gin-gonic/gin"
)

func SetupCatalogRouter(router *gin.RouterGroup) {
	// catalogController := controllers.NewCatalogController()

	// catalogGroup := router.Group("/catalog")
	// {
	// 	catalogGroup.GET("", catalogController.GetCatalogs)
	// 	catalogGroup.GET("/:id", catalogController.GetCatalogByID)
	// 	catalogGroup.POST("", catalogController.PostCatalog)
	// 	catalogGroup.PUT("/:id", catalogController.PutCatalog)
	// 	catalogGroup.DELETE("/:id", catalogController.DeleteCatalog)
	// }
}
